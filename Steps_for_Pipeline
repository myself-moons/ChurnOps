# ChurnOps Pipeline Runbook

This runbook is the canonical setup and execution guide for developers. It
uses the committed DVC graph; do not recreate the stages with `dvc stage add`.

## 1. Requirements

- Python 3.12
- Git
- A local checkout of this repository

The project uses DVC for pipeline reproducibility, MLflow for experiment
tracking, Jenkins for CI, and FastAPI for the web/API layer.

## 2. Install the environment

From the repository root:

```bash
cd ChurnOps
python3.12 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

Confirm the tools are available:

```bash
python --version
dvc --version
mlflow --version
```

Use the same activated environment for every command. DVC stages call
`python`, so using another interpreter can produce missing-package errors.

## 3. Understand the pipeline

Inspect the graph:

```bash
dvc dag
```

The committed order is:

```text
Data_Collection -> Data_Preprocessing -> Model_Training -> Evaluation
```

Run it sequentially:

```bash
dvc repro
```

The data CSVs are DVC outputs and are intentionally not tracked directly by
Git. If they are missing in a clean workspace, `dvc repro` recreates them.

Validate the pipeline and API:

```bash
dvc status
pytest -q
```

## 4. Model selection methodology

ChurnOps uses **5-fold Stratified CV on training data** for model selection.
The test set is held out and evaluated ONCE after the winning model is chosen.

Pipeline:

```text
Training data (80%)
    │
    └── Stratified 5-fold CV
            ├── Logistic Regression  → mean CV ROC-AUC
            ├── Random Forest        → mean CV ROC-AUC
            └── XGBoost             → mean CV ROC-AUC

① Select model with highest mean CV ROC-AUC (test set NOT used here)
② Retrain selected model on COMPLETE training split
③ Evaluate ONCE on untouched test split → final test metrics
④ Save model.pkl
```

## 5. Select and retrain a model

Model settings live in `params.yaml`. The supported model types are:

- `logistic_regression`
- `random_forest`
- `xgboost`
- `all` (trains all three, selects best by CV ROC-AUC)

Example `params.yaml`:

```yaml
model:
  model_type: all
  n_estimators: 200
  max_depth: 8
  random_state: 42
  learning_rate: 0.08
  subsample: 0.95
  colsample_bytree: 0.95
  max_iter: 1000
  C: 1.0
```

After changing parameters, run:

```bash
dvc repro
```

DVC reuses unchanged data stages and reruns training/evaluation. Each training
execution creates new MLflow runs (one per CV candidate + one for the final
model). Previous runs are preserved for comparison.

Commit results:

```bash
git add dvc.lock metrics.json mlruns model.pkl params.yaml
git commit -m "Retrain: update model and metrics"
git push origin main
```

For a one-off trial without editing `params.yaml`:

```bash
python src/model_training.py --model-type all
python src/model_evaluation.py
```

## 6. Inspect MLflow runs

Start the local MLflow UI in a second terminal:

```bash
mlflow ui --backend-store-uri ./mlruns --host 0.0.0.0 --port 5000
```

Open `http://localhost:5000` and select the `customer-churn` experiment.
Compare `model_type`, `cv_mean_roc_auc`, and final `test_roc_auc`.

You can also print the run history:

```bash
python - <<'PY'
import mlflow
from pathlib import Path

mlflow.set_tracking_uri(f"file:{Path.cwd() / 'mlruns'}")
experiment = mlflow.get_experiment_by_name("customer-churn")
runs = mlflow.search_runs(
    experiment_ids=[experiment.experiment_id],
    order_by=["metrics.cv_mean_roc_auc DESC"],
)
print(runs[[
    "run_id", "params.model_type",
    "metrics.cv_mean_roc_auc", "metrics.cv_std_roc_auc",
    "metrics.test_roc_auc",
]].to_string(index=False))
PY
```

For a shared tracking server, set the same URI before running the pipeline:

```bash
export MLFLOW_TRACKING_URI=http://mlflow:5000
dvc repro
```

## 7. Run the FastAPI application

Start the app in another terminal:

```bash
source .venv/bin/activate
python -m uvicorn src.main:app --reload --host 0.0.0.0 --port 8000
```

Pages and endpoints:

| URL | Purpose |
| --- | --- |
| `http://localhost:8000/` | Project overview and pipeline landing page |
| `http://localhost:8000/dashboard` | MLflow run history and tracking dashboard |
| `http://localhost:8000/predict` | Customer churn prediction form |
| `http://localhost:8000/docs` | Interactive API documentation |
| `http://localhost:8000/api/dashboard` | Dashboard data as JSON |
| `http://localhost:8000/api/runs` | MLflow run history as JSON |
| `POST http://localhost:8000/predict` | JSON prediction API |

The prediction form calls the same `POST /predict` API used by clients.

Example API request:

```bash
curl -X POST "http://localhost:8000/predict" \
  -H "Content-Type: application/json" \
  -d '{
    "Gender": "Male",
    "Age": 55,
    "Married": "No",
    "Number of Dependents": 0,
    "Satisfaction Score": 1,
    "Referred a Friend": "No",
    "Number of Referrals": 0,
    "Tenure in Months": 2,
    "Offer": "No Offer",
    "Phone Service": "Yes",
    "Multiple Lines": "No",
    "Internet Service": "Yes",
    "Internet Type": "Fiber Optic",
    "Online Security": "No",
    "Online Backup": "No",
    "Device Protection Plan": "No",
    "Premium Tech Support": "No",
    "Streaming TV": "No",
    "Streaming Movies": "No",
    "Streaming Music": "No",
    "Unlimited Data": "No",
    "Contract": "Month-to-Month",
    "Paperless Billing": "Yes",
    "Payment Method": "Bank Withdrawal",
    "Avg Monthly Long Distance Charges": 0.0,
    "Avg Monthly GB Download": 5,
    "Monthly Charge": 95.0,
    "Total Charges": 190.0,
    "Total Refunds": 0.0,
    "Total Extra Data Charges": 0.0,
    "Total Long Distance Charges": 0.0,
    "Total Revenue": 190.0
  }'
```

## 8. Jenkins

The `JenkinsFile` performs these stages:

1. Checkout
2. Create a Python 3.12 virtual environment
3. Install `requirements.txt`
4. Run `dvc repro`
5. Run `pytest -q`
6. Archive `metrics.json` and `model.pkl`

Create a Jenkins Pipeline job with:

- Definition: `Pipeline script from SCM`
- Repository: this Git repository
- Branch: `main`
- Script path: `JenkinsFile`

The Jenkins agent needs Python 3.12, Git, network access for package
installation, and credentials if the repository is private. For team usage,
configure `MLFLOW_TRACKING_URI` as a Jenkins environment variable pointing to
a persistent MLflow server.

## 9. Common issues

### `dvc: command not found`

Activate `.venv` and install `requirements.txt` again.

### `ModuleNotFoundError: mlflow` or `xgboost`

The command is using a different Python interpreter. Check:

```bash
which python
which dvc
python -m pip show mlflow xgboost
```

### DVC says an output is tracked by Git

Generated `data/raw/*.csv` and `data/processed/*.csv` files must be DVC
outputs, not Git-tracked files. Follow the repository `.gitignore` rules and
run `dvc repro` from the project root.

### Sklearn version warning on unpickling

The model and preprocessor were built with the venv Python.
Re-run the pipeline with the venv activated to regenerate consistent artifacts.
